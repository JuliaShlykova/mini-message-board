extends layout

block content
  h1= title
  for msg in messages 
    div.msg-box
      p #[strong #{msg.user}] #{msg.added}
      p=msg.text
  form(action='/new')
    button(type='submit') Create a new message